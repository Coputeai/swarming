# Resource limits for the isolated API process — same caps as the board
# service, kept separate so neither process's limits are ever confused for
# the other's. See ops/swarming-api.service for install steps.
[Service]
MemoryMax=768M
MemoryHigh=512M
CPUQuota=75%
TasksMax=256
NoNewPrivileges=true
