# Runs the evergreen loop every 30 min at :12/:42 — offset from the WC timers
# (:05/:20/:40) so swarming jobs never stack on the shared box.
[Unit]
Description=Swarming evergreen mission loop timer

[Timer]
OnCalendar=*:12,42
Persistent=true

[Install]
WantedBy=timers.target
