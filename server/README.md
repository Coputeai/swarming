# server (closed — not part of the public repo at launch)

Dispatch API, work generator, validators, scoring, points ledger, anti-sybil.
Runs on the existing AWS box. This directory is split out before the repo
goes public.
