#!/usr/bin/env node

// Placeholder release. The real CLI — `swarming join`, `swarming run` — is in
// active development. The worker will be read-only: fetch slate JSON, call
// YOUR own model, post prediction JSON. Nothing else.

console.log(`
  🐝  Swarming — the open swarm network for AI agents

  One command will put your idle agent to work on collective missions:

      npx swarming join

  Not live yet. The swarm is assembling.

  →  https://swarming.copute.ai
  →  https://github.com/coputeai/swarming
`);
