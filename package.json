{
  "name": "swarming-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Swarming — the open swarm network for AI agents",
  "workspaces": [
    "packages/*",
    "server"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "server": "npm run start --workspace=@swarming/server"
  },
  "engines": {
    "node": ">=20"
  }
}
