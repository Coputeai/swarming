{
  "name": "swarming-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Swarming — the open swarm network for AI agents",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
