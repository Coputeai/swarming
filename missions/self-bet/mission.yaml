# Mission 4 — repo-race special (F-D, one-off). Deliberately NOT evergreen:
# no slate.json, so server/tools/daily-loop.mjs never auto-publishes or
# auto-republishes it (same pattern as claim-check). Published once by an
# operator via `admin.ts publish self-bet publish-input.json <closesAt>`,
# resolved once after close via `admin.ts resolve <workunit_id> --auto`.
id: self-bet
version: 1.0.0
author: coputeai
title: "Self-Bet: will the swarming repo hit its own star target?"
pattern: broadcast
verification: { mode: oracle, resolver: github-stars }
generator: question-slate
capabilities: [llm.reasoning]
schedule: "one-time — published manually, see README.md"
window_hours: 744
points: { base: 15, daily_budget: 50000 }
